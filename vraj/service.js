// document.querySelectorAll('.service-card').forEach(card => {
    //   card.addEventListener('click', () => {
    //     const feature = card.querySelector('h3')?.innerText || 'Unknown feature';
    //     alert(Feature selected: ${feature});
    //   });
    // });